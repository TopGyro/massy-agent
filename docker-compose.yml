services:
  agent:
    build: .
    container_name: massy-agent
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - OPENAI_MODEL=gpt-4o-mini
      - PROJECT_NAME=massy-agent